# @bopstack/lint

Oxlint plugin for Bopstack project conventions.

## What it checks

This package exposes the `bopstack` oxlint plugin with rules for common code-quality and frontend consistency problems:

- `bopstack/no-inline-styles` — report JSX `style={{ ... }}` attributes.
- `bopstack/no-hardcoded-colors` — report literal hex, `rgb()`, `rgba()`, `hsl()`, and `hsla()` colors.
- `bopstack/test-naming` — require `.test.ts` naming instead of `.spec.ts`.
- `bopstack/no-empty-catch` — report empty `catch` blocks.
- `bopstack/no-ts-ignore` — require `@ts-expect-error` instead of `@ts-ignore`.

## Development

Install dependencies with the repo package manager:

```sh
pnpm install
```

Run the available checks through `just`:

```sh
just lint
just format
just build
```

## Build output

The package entrypoint is `dist/index.js`, generated by Rolldown from `src/index.ts`.

## License

MIT
